

## 1. `movies.csv`
Файл в формате CSV, содержащий информацию о фильмах. Традиционно структура подобных файлов включает:


## 2. `occupation.txt`
Текстовый файл, содержащий перечень возможных профессий пользователей. Каждая профессия записана с новой строки.


## 3. `ratings.csv`
Файл в формате CSV, содержащий информацию об оценках, которые пользователи выставили фильмам. Ожидаемая структура:


## 4. `ratings_count.txt`
Текстовый файл, содержащий краткую статистику (сводку) по оценкам или пользователям.
Содержимое: Информация о частоте повторений идентификаторов (например, минимальное id - 1, количество повторений - 232; максимальное id - 120, количество повторений - 22).

## 5. `tags.csv`
Файл в формате CSV, содержащий пользовательские теги к фильмам. Ожидаемая структура:


## 6. `users.txt`
Текстовый файл с подробной информацией о пользователях. Данные разделены символом вертикальной черты (`|`).

## 7. `sqlite.txt`
Текстовый файл, содержащий сведения о версии СУБД SQLite, используемой в проекте.
 Содержимое: Однострочная запись `Версия SQLite 3.35.5`.
Фиксация конфигурации окружения для воспроизводимости и совместимости SQL-запросов.

## 8. `genres.txt`
Текстовый файл, содержащий перечень жанров кинематографа[cite: 1]. Каждая категория записана с новой строки без разделителей[cite: 1].

